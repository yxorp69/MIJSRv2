{
  "name": "mijsrv2",
  "version": "2.0.0",
  "description": "In-browser JavaScript injection tool",
  "scripts": {
    "dev": "esbuild inject.js --bundle --outfile=dist/inject.js --servedir=. --serve=8000 --watch",
    "build": "esbuild inject.js --bundle --minify --outfile=dist/inject.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
